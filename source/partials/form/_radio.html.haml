.row.form-group{"data-wood" => locals[:wood]}
  .col-sm-3
    %label.control-label
      =title
      /=section
    - if locals[:st]
      %div= image_tag '/img/etc/icon-tail.png', title: locals[:st], 'data-toggle' => 'tooltip', style: 'width:40px'      
    
    -section_title = section + "_" + title
  .col-sm-9
    - options.each_with_index do |option, idx|
      .radio
        - id = "#{id_for(section_title)}-#{idx}"
        %label{:for => id}
          %input{id: id, :checked => (1 if idx.zero?), :name => id_for(section_title), :type => "radio", :value => option}

            = option
          - if section == 'body' && title == 'Shape'
            - option_name = option.downcase.gsub(' ', '_')
            %div= image_tag "/img/builds/#{option_name}/#{option_name}.png"

    - id = "#{id_for(section_title)}-other"
    .radio
      %label{:for => id}
        %input{id: id, :name => id_for(section_title), :type => "radio", value: "OTHER", class: 'other'}
          Other 
      .other-value(style="display:none;padding-left:16px")   
        %input{id: id + '-value', :name => (id_for(section_title) + "-other-value"), :type => "text", :placeholder => "Other Value", class: 'input-other-value'}
    

    .text-muted= locals[:hint]

  / .col-sm-6
  /   /%p= locals[:help]
  